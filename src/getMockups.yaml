- meta:
    title: _getMockups
    description: ''
    category: User
    name: IObzdQtVQggUR9CfQiBO4
  steps:
    - type: say
      selected: false
      message: 'Web design rendering prompt:'
    - type: gpt
      prompt: >-
        Taking into consideration the web title, design premises, selected style, selected fonts,
        {{language}} as the selected language, the tone settings for the writer, and the color
        palette configuration, please provide the a prompt that you believe is the most suitable.


        Keep in mind the following guidelines:

        - Use the following example prompt as a template:

          a [goal of website] Website Design of a [product][product values simplified][company
          values simplified] called [company name] aimed at [target audience] who value [target
          audience values simplified]. Combines a [selected style] styles with a [brand color] 
          [Color palette type] [Color range of palette config] palette.
          The titles typography is [selected typographies] and the body typography is [selected
          typographies] written in {{language}} using a [content writer tone] tone.
          --ar 2:3

        - Text inside square brackets is a placeholder. Remove the placeholder text and the square
          brackets to build a proper sentences.

        - "--ar 2:3" is an example of an argument. Please, keep the argument unmodified.

        - Please, no include emojis and no justify your choices.


        Response in English.

        Design premises:

        {{design.premises}}

        Selected Style:

        {{style.selected}}

        Selected fonts:

        {{style.typos}}

        Writer's tone settings:

        {{writer.tone}}

        Color palette configuration:

        {{palette.config}}

        Proposed rendering prompt:
      selected: true
  version: 1
