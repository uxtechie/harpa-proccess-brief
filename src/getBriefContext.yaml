- meta:
    title: zzzGetBriefContext
    description: ''
    category: User
    name: rUcS29e4SijcNrHfIzzv-
  steps:
    - type: gpt
      prompt: >-
        Please format the following data as a JSON structure. Expand the variables in nested
        structures.


        Answer in English.

        Company lang:
          
        {{company.lang}}

        Domain lang:
          
        {{domain.lang}}

        Brief domain:

        {{brief.domain}}

        Web title:

        {{web.title}}

        Design premises:

        {{design.premises}}

        Selected Style:

        {{style.selected}}

        Selected fonts:

        {{style.typos}}

        Writer's tone settings:

        {{writer.tone}}

        Color palette configuration:

        {{palette.config}}

        JSON structure:
      selected: true
  version: 1
