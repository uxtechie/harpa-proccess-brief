- meta:
    title: zzzGetLandingPage
    description: ''
    category: User
    name: euFOoVbKpW_cpJfaxTfkQ
  steps:
    - type: say
      selected: false
      message: 'Proposed landing page:'
    - type: command
      inputs:
        - >-
          - Design premises:
            ```markdown
            {{design.premises}}
            ```

          - Selected style: {{style.selected}}

          - Selected fonts:
            ```markdown
            {{style.typos}}
            ```

          - Color palette:
            ```markdown
            {{palette.config}}
            ```

          - Writer's tone:
            ```markdown
            {{writer.tone}}
            ```

          - Selected layout: {{layout.selected}}
      name: Landing page structure
  version: 1
